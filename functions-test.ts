//imports

//destructured import -> {}
import addNumbers, {addStrings} from "./functions";

console.log(addNumbers(1, 2));
console.log(addStrings("a", "b"));

//Default Parameter || what happens when we need to add another string to addString
console.log(addStrings("a")) //this will give an error. So, we need to modify addString()





//What is any type?
// It could be anything, a string, number, array